import React from "react";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Graphs - WalletTuner",
};

const GraphsPage = () => {
  return <div>GraphsPage</div>;
};

export default GraphsPage;
